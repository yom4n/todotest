# TodoTest - Minimal Next.js Todo App

Simple, minimal Todo list built with Next.js. Stores todos in `localStorage` for demonstration.

Run locally:

```bash
npm install
npm run dev
```

App will be available at http://localhost:3000
