{
  "name": "todotest-demo",
  "private": true,
  "workspaces": [
    "services/gateway"
  ],
  "scripts": {
    "test": "npm test --workspace services/gateway",
    "dev": "docker compose up",
    "seed": "node services/load/generate-load.js"
  }
}
